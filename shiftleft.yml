build_rules:
  - id: allow-zero-findings
    finding_types:
      - vuln
      - secret
      - insight
      - "*"
    severity:
      - critical
      - high
      - medium
      - low
    threshold: 0
